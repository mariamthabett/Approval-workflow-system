# Backend API base URL. Baked in at build time (Vercel env var).
# Point this at your hosted .NET backend, e.g. https://your-app.onrender.com
# Leave empty to require the runtime override (⚙️ Backend URL button / ?api=...).
NEXT_PUBLIC_API_BASE=
